export type ConfigTypes = {
  PORT: number;
  JWT_SECRET: string;
  MONGODB_URI: string;
  REDIS_USERNAME: string;
  REDIS_PASSWORD: string;
  REDIS_HOST: string;
  REDIS_PORT: number;
  REDIS_DB: number;
};
